
const WordDisplay = ({userWord}) => {
    
    return(
        <div>
            WordDisplay
            <div>here is the userWord:{userWord}</div>
        </div>
    );
}

export default WordDisplay;